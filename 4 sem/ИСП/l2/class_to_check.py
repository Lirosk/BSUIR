import math

class Ctc:
    _s1 = 's1'
    _s2 = 2

    def __init__(self):
        self._a1 = 'a1'
        self._a2 = 3

    def m(n):
        return math.erf(n)

    def fff(self, n):
        self._s2 += 1
        self._a1 += n

    _l = ["list", 103131, True, None, False]
    _d = {12: 23, "312": (lambda x :((l := lambda y: (l(y-1) + l(y-2)) if y > 2 else (1 if y == 2 else 0))(x)))}
    l = (lambda x :((l := lambda y: (l(y-1) + l(y-2)) if y > 2 else (1 if y == 2 else 0))(x)))